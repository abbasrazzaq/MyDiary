<Window x:Class="MyDiary.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyDiary"
        mc:Ignorable="d"
        Title="Diary Locked!" Height="200" Width="600">
    <Grid>
        <StackPanel>

            
            <Grid VerticalAlignment="Center" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Label Grid.Column="0" Grid.Row="0"  Content="Name:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <TextBox Grid.Column="1" Grid.Row="0" x:Name="usernameTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Center" />

                <Label Grid.Column="0" Grid.Row="1" Content="Password:" VerticalAlignment="Center" Margin="0,0,5,0" />
                <PasswordBox Grid.Column="1" Grid.Row="1" x:Name="passwordBox" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
            </Grid>

            <Button x:Name="loginButton" Click="loginButton_Click" Width="Auto" HorizontalAlignment="Center" Padding="5" Content="Unlock Diary" />
        </StackPanel>
        
    </Grid>
</Window>
